## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary mapping to canonical roles (`needs-triage`, `ready-for-agent`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (one `CONTEXT.md` at root). See `docs/agents/domain.md`.
